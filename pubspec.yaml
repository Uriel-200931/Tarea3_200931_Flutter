name: tarea3_200931
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2 # Debes especificar la versión deseada

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/image.png
    - assets/icono.jpg
    
  fonts:
    - family: MiFuente
      fonts:
        - asset: assets/mifuente.ttf
  

